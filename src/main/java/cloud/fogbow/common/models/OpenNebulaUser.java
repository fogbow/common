package cloud.fogbow.common.models;

public class OpenNebulaUser extends CloudUser {
    public OpenNebulaUser(String userId, String userName, String tokenValue) {
        super(userId, userName, tokenValue);
    }
}
