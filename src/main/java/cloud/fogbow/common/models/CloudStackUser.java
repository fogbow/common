package cloud.fogbow.common.models;

public class CloudStackUser extends CloudUser {
    public CloudStackUser(String userId, String userName, String tokenValue) {
        super(userId, userName, tokenValue);
    }
}
